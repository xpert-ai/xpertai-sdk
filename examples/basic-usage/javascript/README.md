# XpertAI Javascript basic usage

1. Copy the `.env.example` file to `.env` and fill in your API credentials.

2. Run the following command to install dependencies:
    ```
    npm install
    ```

3. Start the application:
    ```
    npm start
    ```
